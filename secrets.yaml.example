# 프로덕션/배포용 암호화된 비밀 설정 템플릿
# SOPS로 암호화되어 Git에 저장됨
# 실제 파일명: secrets.enc.yaml

# API 키 설정 (실제 환경에서는 암호화)
api_keys:
  gemini: 'YOUR_GEMINI_API_KEY'
  openai: 'YOUR_OPENAI_API_KEY'
  claude: 'YOUR_CLAUDE_API_KEY'

# 기타 설정
config:
  log_level: 'INFO'
  debug: false
